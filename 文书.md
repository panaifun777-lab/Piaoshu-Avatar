# 飘叔 Avatar OS — Vercel 部署正确方式 v1.0

> 2026-06-12 验证通过 · 本地 + Vercel 全线可用

---

## 一、GitHub 同步

```bash
# 仓库
github.com/panaifun777-lab/Piaoshu-Avatar.git

# 推送（强制覆盖远程）
git add -A
git commit -m "your message"
git push origin main --force
```

`.gitignore` 必须包含：
```
.env
.env.local
node_modules/
.next/
prisma/*.db
.codex-history/
.vscode/
```

---

## 二、Vercel 环境变量

登录 [Vercel Dashboard](https://vercel.com) → 项目 `piaoshu-avatar` → Settings → Environment Variables：

| Key | Value |
|:---|:---|
| `DATABASE_URL` | `file:/tmp/dev.db` |
| `NEXTAUTH_URL` | `https://piaoshu-avatar.vercel.app` |
| `NEXTAUTH_SECRET` | `piaoshu-founder-os-secret-key-v1` |
| `VERCEL` | `1` |

设置后点击 **Redeploy** 重新构建。

---

## 三、部署配置要点

### vercel.json
```json
{
  "framework": "nextjs",
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install"
}
```

### 关键技术修复

#### 1. 数据库（Vercel serverless 不支持文件型 SQLite）
- `src/lib/db.ts` — 检测 Vercel 环境自动切换 `DATABASE_URL` 为 `file:/tmp/dev.db`
- `src/lib/vercel-db-init.ts` — 运行时用 `$executeRawUnsafe` 直接 SQL 建表（User/Founder/AuditLog/AvatarClone/CloneAgent/CloneSkill），不依赖慢 `prisma db push`

#### 2. NextAuth 登录跳转
- `src/lib/auth.ts` — NextAuth `authorize` 回调中调用 `initVercelDb()` 确保首次登录前 DB 已初始化
- `src/components/piaoshu/auth-modal.tsx` — 登录/注册成功后执行 `window.location.reload()` 强制刷新，确保 `useSession` 检测到新 session

#### 3. SpeedInsights 依赖缺失
- 删除 `src/app/layout.tsx` 中 `import { SpeedInsights } from '@vercel/speed-insights/next'` 和 `<SpeedInsights />`

#### 4. 构建兼容
- `next.config.ts` — Vercel 环境不设置 `output: "standalone"`
- `vercel.json` — `installCommand: "npm install"`

---

## 四、首次部署后操作

1. **初始化管理员**：访问 `POST /api/auth/setup-admin`
   - 自动创建 `Piaoshu001` 管理员 + Founder + 4个 CloneAgent + 6个 CloneSkill
2. **登录**：首页点击「登录」→ 输入 `Piaoshu001` / `Gai169999$`
3. 登录成功后自动跳转到后台仪表盘

---

## 五、已知限制

| 问题 | 说明 | 生产环境方案 |
|:---|:---|:---|
| `/tmp` 数据冷启动丢失 | Vercel serverless 每函数实例独立 `/tmp`，重启后数据清空 | 配置 [Turso](https://turso.tech) 数据库，设置 `DATABASE_URL=libsql://...` |
| Blockchain 服务 500 | 本地区块链 mini-service 未在 Vercel 运行 | 部署独立区块链节点服务 |
| Logo 视频不 autoplay | 浏览器禁止自动播放音视频 | 点击页面后触发播放，已内置 emoji 降级 |
| 13MB logo 首次加载慢 | 未做视频压缩/优化 | 生成 webm 压缩版 + poster 缩略图 |

---

## 六、本地开发

```bash
# .env
DATABASE_URL="file:./prisma/dev.db"
NEXTAUTH_SECRET="piaoshu-founder-os-secret-key-v1"
NEXTAUTH_URL="http://localhost:3000"

# 启动
npm install
npx prisma db push
npx next dev -p 3000
```

---

_最后更新：2026-06-12 23:12 · 验证人：飘叔_
