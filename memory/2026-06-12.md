# 2026-06-12 Daily Notes

## 飘叔 Avatar OS — GitHub 同步 & Vercel 部署 & Logo

### 本地开发环境启动
- 清理旧 `node_modules`（路径锁死问题），`npm install` 重新安装
- 创建 `.env`，设置 `DATABASE_URL=file:./prisma/dev.db`
- `npx prisma db push` 初始化数据库
- `next dev -p 3000` 启动，Turbopack 编译正常
- 自动写入种子数据：admin 用户 + Founder + 4 CloneAgent + 6 CloneSkill
- 16 个功能模块全部可访问

### Github 推送
- 修复 `.gitignore`：新增 `.env`、`node_modules/`、`.next/`、`prisma/*.db`、`.codex-history/`、`.vscode/`
- Repo: `panaifun777-lab/Piaoshu-Avatar`，branch: `main` (force push)
- Git remote 已设置 Token 认证

### Vercel 部署修复
- **根因**：Vercel serverless 无法使用 `file:./dev.db` SQLite；`npx prisma db push` 在 serverless 中太慢（30s timeout）
- **修复1**：`src/lib/db.ts` — Vercel 环境自动切换 `DATABASE_URL` 为 `file:/tmp/dev.db`
- **修复2**：`src/lib/vercel-db-init.ts` — 直接 SQL 建表（`$executeRawUnsafe`），替代慢的 `prisma db push`
- **修复3**：`src/lib/auth.ts` — NextAuth `authorize` 回调中自动调 `initVercelDb()`
- **修复4**：`vercel.json` — `installCommand` 改为 `npm install`（Vercel 兼容）
- **修复5**：移除 Vercel Speed Insights 自动 PR 引入的缺失依赖 `@vercel/speed-insights`
- **需手动设置 Vercel 环境变量**：`NEXTAUTH_URL=https://piaoshu-avatar.vercel.app`，`NEXTAUTH_SECRET=piaoshu-founder-os-secret-key-v1`
- **验证结果**：setup-admin API 201 创建管理员成功，登录鉴权通过，仪表盘加载成功

### Logo 视频集成
- 视频源：`C:\Users\Administrator\Desktop\avatar  logo.mp4`（13MB）
- 复制到 `public/avatar-logo.mp4`
- 新建 `src/components/piaoshu/piaoshu-logo.tsx`：
  - `PiaoshuLogo` — 通用 Logo 组件（sm/md/lg/hero 尺寸）
  - `PiaoshuLogoSidebar` — 侧边栏专用紧凑版
  - autoplay muted loop，播放失败降级到 🧬 emoji
- 已集成位置：侧边栏 Logo、加载屏、login dialog、首页 Header、首页 Footer

### 技术细节
- Prisma schema 使用 SQLite provider
- Vercel 生产环境建议用 Turso 数据库（`libsql://` URL，已预留支持）
- Vercel `/tmp` 数据冷启动丢失，仅适合演示
- 本地 blockchain mini-service 未启动导致证据链页 500，不影响核心功能

### Git commit 链
1. `ea921de` — 初始推送（修复 .gitignore）
2. `1d2855a` — Logo + Vercel DB init fix（含 SpeedInsights merge）
3. `dacbb63` — 直接 SQL 建表替代 prisma db push + 移除 SpeedInsights
